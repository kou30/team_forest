CREATE TABLE    reserve(
    userName    varchar(10) not null
    ,id         int     auto_increment  primary key
    ,catId      char(6)  
    ,Date       date        not null
    ,comment    varchar(100)
    ,foreign key(catId)   references cat(id)
);


insert
    into    reserve(userName, id, catId, date, comment)
    values  ('–k–ì', 0, 111, '2022-07-25', 'ŽO–Ñ”L‚Í˜a•—‚Å‚¢‚¢‚Å‚·‚Ë'
);

insert
    into    reserve(userName, id, catId, date, comment)
    values  ('–k–ì', 0, 11, '2022-07-25', 'ŽO–Ñ”L‚Í˜a•—‚Å‚¢‚¢‚Å‚·‚Ë'
);
    
drop table
    reserve
;
